{% extends '@EMSCH/template/blocks/abstract-block.html.twig' %}

{% block widget %}
    {{ render_esi(controller('emsch.controller.embed::fragment', {
        template: '@EMSCH/template/fragments/last_updates.html.twig',
        context: {
            trans_default_domain: trans_default_domain,
            limit: item.object.limit
        },
    })) }}
{% endblock %}
